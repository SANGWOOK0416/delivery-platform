# Vite only exposes env vars prefixed with VITE_ to client code. Copy this file to
# .env.local (gitignored) to override; both already default to the right ports locally.
VITE_ORDER_SERVICE_URL=http://localhost:8081
VITE_NOTIFICATION_SERVICE_URL=http://localhost:8083
